<p>
  Turns left: <%= @tally.turns_left %>
</p>

<p>
  Letters used: <%= letters(@tally.used) %>
</p>

<p>
  <%= letters(@tally.letters) %>
</p>

<%= if game_over?(@tally) do %>
  <%= button "New game", to: Routes.hangman_path(@conn, :create) %>
<% else %>
  <%= form_for(@conn, Routes.hangman_path(@conn, :make_move), [ as: :make_move, method: :put ], fn f -> %>
    <%= text_input(f, :guess, value: "", autofocus: true) %>
    <%= submit("Make next move") %>
  <% end) %>
<% end %>
